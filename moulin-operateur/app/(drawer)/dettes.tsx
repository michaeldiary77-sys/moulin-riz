import { Text, View } from 'react-native';

export default function DettesScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Écran Dettes (temporaire, sera remplacé par le contenu existant)</Text>
    </View>
  );
}
